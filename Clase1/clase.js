const vars = [1, 2, 4, 8];

for 



















/*
function suma() {
    let suma = 0;
    for (numeroActual of vars) {
        suma += numeroActual;
        console.log(suma)
    }
}

suma();
*/
/*
vars.forEach(
    (numeroActual) => {
        suma = suma + numeroActual;
        console.log('suma con for : ' + suma)
    }
)
*/

/*
for(i=0;i<vars.length;i++){
    suma = suma + vars[i]
    console.log('suma con for : ' + suma)
}
*/

/*
console.log('suma con reduce : '+vars.reduce((numeroActual,numeroSiguiente) => numeroActual + numeroSiguiente))
*/